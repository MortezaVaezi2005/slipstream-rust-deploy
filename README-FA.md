# Slipstream Rust Plus Deploy 🚀

[![Build](https://img.shields.io/github/actions/workflow/status/Fox-Fig/slipstream-rust-deploy/release.yml?style=flat-square)](https://github.com/Fox-Fig/slipstream-rust-deploy/actions)
[![Version](https://img.shields.io/github/v/release/Fox-Fig/slipstream-rust-deploy?style=flat-square)](https://github.com/Fox-Fig/slipstream-rust-deploy/releases)
[![License](https://img.shields.io/badge/License-GPLv3-blue.svg?style=flat-square)](LICENSE)

[**🇺🇸 English**](README.md) | [**🇮🇷 فارسی**](README-FA.md)



<div dir="rtl">

**اتوماسیون دیپلوی پیشرفته برای پروژه [Slipstream Rust Plus](https://github.com/Fox-Fig/slipstream-rust-plus)**.
یک اسکریپت جامع برای راه‌اندازی و مدیریت سرورهای تونل DNS پرسرعت Slipstream روی لینوکس و اندروید. این اسکریپت تمام مراحل از بیلد سورس تا کانفیگ نهایی را به صورت خودکار انجام می‌دهد.

---

> [!IMPORTANT]
> **هشدار سازگاری (Compatibility Warning)**
> این ریپازیتوری یک فورک تخصصی متصل به هسته‌ی **[Slipstream Rust Plus](https://github.com/Fox-Fig/slipstream-rust-plus)** است.
> کلاینت‌هایی که توسط این اسکریپت دانلود یا بیلد می‌شوند **فقط** با سرورهای `slipstream-rust-plus` سازگار هستند.
> لطفاً از استفاده این کلاینت‌ها با سرورهای [slipstream-rust](https://github.com/Mygod/slipstream-rust/) خودداری کنید، زیرا تفاوت‌های پروتکلی ممکن است باعث عدم اتصال شود.

---

## 🌟 ویژگی‌های پیشرفته (نسخه Plus)

این پروژه اسکریپت اصلی دیپلوی را با قابلیت‌های ویژه‌ای توسعه داده است:

- **⚡ هسته بهینه شده**: استفاده از موتور [slipstream-rust-plus](https://github.com/Fox-Fig/slipstream-rust-plus) که دارای ویژگی‌های **Turbo Mode** و مدیریت پیشرفته فشار (Backpressure) برای دستیابی به بالاترین سرعت ممکن است.
- **🛡️ مدیریت قدرتمند DNS**: تاکید بر استفاده از **چندین DNS Resolver**. به کاربران شدیداً توصیه می‌شود که چندین ریزالور بالادستی (Upstream) تنظیم کنند تا آپتایم ۱۰۰٪ داشته باشند و از مسمومیت DNS جلوگیری کنند.
- **📱 پشتیبانی بومی ترموکس (Termux)**: دارای یک بیلد اختصاصی برای **اندروید (arch arm64)**. شما می‌توانید یک سرور کامل و قدرتمند را مستقیماً روی گوشی خود اجرا کنید!
- **🔢 ورژن‌بندی عددی**: پیاده‌سازی سیستم ورژن‌بندی دقیق و افزایشی (مثل 1.01، 1.02) برای آپدیت‌های منظم.
- **🔄 آپدیت خودکار**: اسکریپت خود-آپدیت‌شونده که تضمین می‌کند همیشه آخرین پچ‌ها و باینری‌ها را دارید.

---

## تنظیمات دامنه DNS

قبل از استفاده از اسکریپت، باید رکوردهای DNS دامنه خود را به درستی تنظیم کنید:

### نمونه تنظیمات
- **نام دامنه شما**: `example.com`
- **آی‌پی سرور شما**: `203.0.113.2`
- **زیردامنه تونل**: `s.example.com`
- **هاست‌نیم سرور**: `ns.example.com`

### تنظیم رکوردها
وارد پنل مدیریت دامنه خود شوید و رکوردهای زیر را اضافه کنید:

| نوع (Type) | نام (Name) | مقدار (Points to) |
|------------|------------|-------------------|
| A | `ns.example.com` | `203.0.113.2` |
| NS | `s.example.com` | `ns.example.com` |

**مهم**: قبل از تست تونل، صبر کنید تا تغییرات DNS اعمال شود (ممکن است تا ۲۴ ساعت طول بکشد).

## 📥 شروع سریع (نصب)

### پیش‌نیازها
- سرور لینوکس (CentOS, Debian, Ubuntu, Fedora) یا دستگاه اندروید (Termux)
- دسترسی روت (Root) یا sudo
- اتصال اینترنت
- **دامنه تنظیم شده** (طبق راهنمای بالا)

### دستور نصب (One-command)

```bash
bash <(curl -Ls https://raw.githubusercontent.com/Fox-Fig/slipstream-rust-deploy/master/slipstream-rust-deploy.sh)
```

این دستور مراحل زیر را انجام می‌دهد:
1. دانلود و نصب اسکریپت در `/usr/local/bin/slipstream-rust-deploy`
2. دانلود باینری آماده (یا بیلد از سورس در صورت نیاز)
3. شروع فرآیند نصب تعاملی
4. کانفیگ خودکار سرور

### پلتفرم‌های پشتیبانی شده
- **لینوکس (x86_64)**: سرورهای استاندارد
- **لینوکس (ARM64)**: رزبری پای، اوراکل کلود
- **اندروید (ARM64)**: ترموکس (بیلد اختصاصی)
- **مک‌او‌اس**: برای تست لوکال

## 🛠️ نصب روی ترموکس (Android Termux)

اجرای سرور روی اندروید اکنون آسان‌تر از همیشه است:

1. برنامه **Termux** را (ترجیحاً از F-Droid) نصب کنید.
2. پکیج‌ها را آپدیت کنید: `pkg update && pkg upgrade`.
3. ابزارهای لازم را نصب کنید: `pkg install curl proot`.
4. دستور نصب بالا را اجرا کنید.
   > اسکریپت به صورت هوشمند محیط اندروید را تشخیص داده و **باینری بهینه شده برای اندروید** را دانلود می‌کند.

## ⚙️ بهترین روش‌های پیکربندی

### استفاده از چندین Resolver
برای داشتن بیشترین پایداری و سرعت، حتماً چندین **DNS Resolver** مختلف را در تنظیمات خود وارد کنید. این افزونگی (Redundancy) به سرور اجازه می‌دهد تا درخواست‌ها را توزیع کرده و در صورت قطعی یا کندی یک سرویس‌دهنده، بلافاصله از دیگری استفاده کند.

### تنظیمات پرفرمنس
نسخه **Plus** شامل حالت‌های آزمایشی "Ultra Turbo" است. در دستگاه‌هایی با رم پایین (مثل برخی گوشی‌ها) با احتیاط استفاده کنید.

## استفاده از کلاینت (Client Usage)

پس از راه‌اندازی سرور، می‌توانید متصل شوید.
> [!NOTE]
> حتماً از کلاینت‌های سازگار با **[Slipstream Rust Plus](https://github.com/Fox-Fig/slipstream-rust-plus)** استفاده کنید.

### کلاینت خط فرمان (Command-Line)
برای کاربران حرفه‌ای:

#### توصیه مهم
به شدت توصیه می‌شود برای پایداری بیشتر از **حداقل دو DNS Resolver** استفاده کنید. برای این کار کافیست پرچم `--resolver` را چندین بار تکرار کنید.

#### شروع سریع (لینوکس)
```bash
# دانلود کلاینت
curl -Lo slipstream-client https://github.com/Fox-Fig/slipstream-rust-deploy/releases/latest/download/slipstream-client-linux-amd64
chmod +x slipstream-client

# اجرا (اتصال به سرور) با استفاده از چندین ریزالور
./slipstream-client --resolver 1.1.1.1:53 --resolver 8.8.8.8:53 --domain s.example.com
```

#### گزینه‌های مهم کلاینت
| گزینه | توضیحات | مثال |
|-------|---------|------|
| `--resolver`, `-r` | آدرس دی‌ان‌اس ریزالور (آی‌پی سرور یا عمومی) | `--resolver 8.8.8.8:53` |
| `--domain`, `-d` | دامنه تونل | `--domain s.example.com` |
| `--tcp-listen-port`, `-l` | پورت لوکال کلاینت | `-l 5201` |
| `--keep-alive-interval`, `-t` | فاصله زمانی Keep-alive | `-t 500` |

## مدیریت و رفع اشکال

### منوی مدیریت
ساده‌ترین راه مدیریت سرور استفاده از منوی تعاملی است:
```bash
slipstream-rust-deploy
```
این منو امکاناتی مثل تغییر تنظیمات، آپدیت اسکریپت، و مشاهده لاگ‌ها را فراهم می‌کند.

### حالت‌های تونل (Tunnel Modes)
1. **SOCKS Mode**: پروکسی ساکس نسخه ۵ (پورت ۱۰۸۰).
2. **SSH Mode**: تونل کردن ترافیک DNS به سرویس SSH.
3. **Shadowsocks Mode**: تونل کردن ترافیک به شادوساکس لوکال.

### رفع اشکال رایج
- **سرویس اجرا نمی‌شود**: لاگ‌ها را چک کنید: `journalctl -u slipstream-rust-server -f`.
- **خطای بیلد**: مطمئن شوید پیش‌نیازهایی مثل `cargo` و `openssl` نصب هستند.

## 🙏 قدردانی

این پروژه فورکی از کار عالی [AliRezaBeigy](https://github.com/AliRezaBeigy/slipstream-rust-deploy) است. ما از زحمات ایشان قدردانی می‌کنیم.

## 📄 لایسنس

این پروژه تحت لایسنس **GNU General Public License v3.0 (GPLv3)** منتشر شده است.
برای جزئیات بیشتر فایل [LICENSE](LICENSE) را مشاهده کنید.

</div>

---
<div align="center">
  <p>با ❤️ در <a href="https://t.me/foxfig">FoxFig</a> ساخته شد</p>
  <p>تقدیم به تمام مردم ایران 🇮🇷</p>
</div>
